version: '3'
services:
  api-gateway:
    build: ./api-gateway
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production

# These services are commented out as they're not implemented yet
# Uncomment and adjust when you're ready to add these services

#  user-service:
#    build: ./user-service
#    environment:
#      - PORT=3001

#  task-service:
#    build: ./task-service
#    environment:
#      - PORT=3002

networks:
  default:
    driver: bridge